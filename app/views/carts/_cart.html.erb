<%= render(cart.line_items)%>
<hr>
<strong>Total:&nbsp;<%=number_to_currency(@cart.total_price)%></strong>
<%= button_to 'Empty Cart', @cart, method: :delete, data:{comfirm: "Are you sure ?"}, class: "btn btn-danger"%>



